{
  "name": "bms",
  "version": "1.0.0",
  "description": "book my show",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build":"npm install && cd .. && cd client && npm install && npm run build",
    "server":"node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "nodemon": "^3.0.2",
    "stripe": "^14.10.0"
  }
}
